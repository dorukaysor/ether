[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA-style redirects — sends all routes to index.html so Astro can handle them
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
